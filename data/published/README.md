# Published Data

This directory contains the latest approved publishable program only.

- `current-program.json`
  - The active approved release used as the current publishable source.
- `current-release.json`
  - Metadata pointer for the latest approved release.

This directory is mutable and should only contain the current approved state, not archived history.
