# EventLive — الخطة التطويرية الشاملة (Master Development Plan)

> **الهدف**: أن تكون eventme.live الوجهة الأولى والمرجع الوطني للفعاليات في السعودية — خصوصًا **وقت الفعالية نفسها** — لكل فئات المجتمع: الجنسين، الطلاب، الباحثين عن عمل، الموظفين، التقنيين، أصحاب المهارات والحرف، العائلات، رواد الأعمال، والباحثين.
>
> **المنفّذ**: Codex (أو أي وكيل تنفيذي). **المالك**: بدر. **التقدّم يقاس ببوابات PASS/FAIL لا بالأيام.**
>
> هذه الوثيقة تبني على `EVENTME-PRODUCT-STRATEGY.md` و`EVENTME-SOURCE-ACQUISITION.md` ولا تلغيهما. عند أي تعارض: قواعد الحوكمة في هذه الوثيقة (§0) هي الحاكمة.
>
> طبقة الجلب الدوري وتشغيل المصادر يحكمها أيضًا `EVENTME-SOURCE-OPERATING-MODEL.md`: الجلب الخام ليس نشرًا، ومصادر الاكتشاف لا تقفز إلى الكتالوج، وكل دورة تترك `data/source_run_state.json` كتقرير حالة قابل للاستئناف.

---

## §0 — قواعد حاكمة لا تُكسر (اقرأها قبل أي سطر كود)

1. **الدومين والهوية**: `eventme.live` والاسم `EventLive`. لا دومين آخر، لا تغيير هوية في الروابط العامة.
2. **الأتمتة أولًا**: كل ميزة تُصمَّم لتعمل دوريًا بلا تدخل يدوي. المراجعة اليدوية استثناء موثّق، ليست مسارًا أساسيًا.
3. **بوابة الثقة**: مصادر الاكتشاف المجتمعية/العامة (Eventbrite, Eye of Riyadh, أدلة عامة) **لا تنشر مباشرة أبدًا** — إشارات اكتشاف فقط. النشر الآلي حصري لمصادر official/approved عبر بوابة auto-publish القائمة.
4. **قاعدة event_kind المحمية**: البرنامج الممتد (نافذة > 14 يومًا) لا يظهر "مباشرة الآن" أبدًا؛ حالته `ongoing`. البوابة: `npm run test:event-kind`.
5. **بعد أي تغيير مؤثر**: `npm run test:csv` ثم `npm run validate` ثم `npm run build` ثم بقية اختبارات الانحدار. أي ميزة جديدة = اختبار انحدار جديد يحميها.
6. **البنية**: موقع ساكن (GitHub Pages) — لا خادم، لا قاعدة بيانات، لا مفاتيح مدفوعة. الشخصنة عبر localStorage/PWA. أي اقتراح يتطلب backend يُسجَّل في §11 (الشراكات/اللاحق) ولا يُنفَّذ الآن.
7. **`published: 0` مع `linked_existing > 0` ليس فشلًا** — هو نظام يعمل. لا "تصلحه".
8. **احترام المصادر**: robots.txt، معدلات جلب متحفظة، User-Agent واضح، snapshot كدليل. لا تجاوز لأي حماية.
9. **لا commit إلا بطلب المالك** (أو ضمن خطوة CI الموثقة في source-sync.yml).

**أوامر التحقق القياسية** (شغّلها بعد كل مرحلة):
```bash
npm run test:event-kind && npm run test:csv && npm run validate \
&& npm run build && npm run test:validation \
&& npm run test:source-auto-publish && npm run test:source-health-gate \
&& npm run sources:health-gate
```

---

## §1 — أين نحن الآن (خط الأساس، 2026-07-04)

| المؤشر | القيمة |
|---|---|
| مصادر مسجلة | 54 (24 official-feed-preferred، 19 monitor-public، 8 candidate-only، 3 partnership) |
| جامعات نشطة | 20 (تغطية 37%) |
| الكتالوج | 46 فعالية: 32 لحظية / 14 برنامجًا ممتدًا |
| جداول حية جاهزة | **1 فقط** ← أضعف نقطة في وعد المنتج |
| مرشحون | 60 (45 مربوطة، 15 محجوبة اكتشافيًا) |
| مزامنة | يومية 06:17 صباحًا بتوقيت الرياض + استمرارية حالة إلى git |
| مدن مغطاة | 7 فقط، أغلبها رياض/ظهران ← فجوة جدة/مكة/المدينة/الشرقية الترفيهية |

**الفجوات الخمس الكبرى** (كل المراحل أدناه تعالجها):
1. حجم التغطية صغير (46) ونصيب الفعاليات الجماهيرية اللحظية ضعيف — الكتالوج يميل للتدريب التقني.
2. لا توجد طبقة فئات جمهور (من هذه الفعالية؟ لمن؟).
3. الجدول الحي (جوهر المنتج) مفعَّل لفعالية واحدة.
4. لا بحث، لا صفحات زمنية (اليوم/الأسبوع/الويكند)، اكتشاف محدود.
5. لا قياس (analytics) ولا قنوات رجوع (اشتراك تقويم، RSS، مشاركة غنية).

---

## §2 — المرحلة A: طبقة فئات الجمهور (Audience Layer) — **الأولوية 1**

**لماذا أولًا**: "لكل فئات المجتمع" تتطلب أن يجد كل شخص صفحته خلال ثوانٍ. هذه الطبقة رخيصة، تلمس كل الواجهة، وتضاعف قيمة كل فعالية موجودة وقادمة.

### A.1 التصنيف (taxonomy) — حقل `audiences[]` على كل فعالية
قيم معيارية (slug إنجليزي + تسمية عربية):

| slug | التسمية | إشارات التصنيف الآلي |
|---|---|---|
| `students` | طلاب وخريجون | جامعة، طلاب، خريج، bootcamp مجاني، تدريب تعاوني |
| `job-seekers` | باحثون عن عمل | توظيف، career، ملتقى مهني، سيرة ذاتية، تأهيل لسوق العمل |
| `professionals` | موظفون ومهنيون | قيادة، إدارة، شهادات مهنية، CPD، ملتقى قطاع |
| `tech` | تقنيون | برمجة، ذكاء اصطناعي، سايبر، بيانات، SDAIA، MCIT، هاكاثون |
| `skilled-trades` | مهاريون وحرفيون | حرف، مهارات، أشغال، تصنيع، لحام، كهرباء، Skills |
| `entrepreneurs` | رواد أعمال | منشآت، ريادة، مسرعة، حاضنة، استثمار جريء |
| `researchers` | باحثون وأكاديميون | مؤتمر علمي، ورقة بحثية، ندوة أكاديمية، KAUST، مكتبات |
| `families` | عائلات وأطفال | عائلي، أطفال، مواسم، ترفيه، مهرجان |
| `women` | فعاليات نسائية | نسائي، سيدات (تُضاف فقط عند نص صريح في المصدر) |
| `creatives` | مبدعون وفنون | فنون، تصميم، سينما، مسرح، موسيقى، إثراء، أدب |
| `sports` | رياضة ولياقة | دوري، بطولة، جري، لياقة، ماراثون |
| `general` | عموم الجمهور | الافتراضي عند غياب إشارة أقوى |

### A.2 التنفيذ
1. `scripts/audience-utils.mjs`: مصنّف قواعدي (title + summary + tags + category + source) يرجع `audiences[]` (واحدة على الأقل، `general` احتياطًا). كلمات عربية **وإنجليزية**، مع تطبيع عربي (همزات/تاء مربوطة/ال التعريف).
2. تمريره في `normalizeCatalogEvent` وفي `collect-source-candidates.mjs` (المرشح يولد بفئاته من المنبع).
3. تحديث `data/events-catalog.schema.json` و`data/source-candidates.schema.json` (حقل اختياري compatible مع الموجود، والـ validate يمر).
4. **صفحات جمهور** `dist/for/<slug>.html` (نفس نمط صفحات المدن): بطاقة بطل بعنوان الفئة + الفعاليات مرتّبة (live→upcoming→ongoing) + عدّادات + روابط تقويم.
5. فلتر "الفئة" في الرئيسية + شارات الفئة على البطاقات + روابط الفئات في التذييل وsitemap.
6. `dist/audiences.json` (فهرس آلي للفئات وأعدادها).

### A.3 بوابة القبول (Gate A)
- [ ] 100% من فعاليات الكتالوج تحمل `audiences[]` غير فارغة.
- [ ] `scripts/audience-regression-test.mjs` + سكربت npm `test:audience`: حالات عربية/إنجليزية/مختلطة + حالة "لا إشارة → general" + حماية: فعالية توظيف لا تُصنَّف عائلات.
- [ ] صفحات `for/*` مبنية ومربوطة من الرئيسية وsitemap.
- [ ] كل أوامر التحقق القياسية PASS.

---

## §3 — المرحلة B: توسيع التغطية — محرّكات الاستخراج الرسمية — **الأولوية 2**

**لماذا**: المرجعية = تغطية. 46 فعالية لا تصنع مرجعًا وطنيًا. الوقود هو extractors لمصادر رسمية عالية الإنتاج، مع الحفاظ على البوابات.

### B.1 ترتيب البناء (من `next_extractors` في source-health + قيمة الجمهور)
| # | المصدر | لماذا | فئات الجمهور المتوقعة |
|---|---|---|---|
| 1 | **Visit Saudi Calendar/Seasons** | أكبر مصدر فعاليات جماهيرية لحظية رسمي | families, creatives, sports, general |
| 2 | **Ithra Events** | برمجة ثقافية غنية شهرية، صفحات مفصلة | creatives, families, researchers |
| 3 | **SDAIA Academy** | برامج تقنية حكومية | tech, students, job-seekers |
| 4 | **Monsha'at** | ملتقيات وبرامج ريادة | entrepreneurs, job-seekers |
| 5 | **Saudi Pro League / وزارة الرياضة** | جماهيري أسبوعي متجدد | sports, families |
| 6 | **RFECC / RICEC (مراكز المعارض)** | معارض B2B/B2C بتواريخ دقيقة | professionals, general |
| 7 | **الجامعات الكبرى** (KSU, KAU, KFUPM, KAUST) | مؤتمرات وأيام مهنية | students, researchers, job-seekers |
| 8 | **الغرف التجارية** (رياض، جدة، شرقية) | ملتقيات أعمال وتوظيف | professionals, job-seekers, entrepreneurs |
| 9 | **هيئة تنمية الموارد البشرية / طاقات-هدف** | ملتقيات توظيف | job-seekers ← فئة حرجة |
| 10 | **MDLBEAST / موسم الرياض / موسم جدة** | ترفيه ضخم | general, families |

### B.2 قواعد بناء كل extractor (نمط موحد)
1. ابدأ بـ`npm run sources:probe` للمصدر؛ ابنِ فقط إذا ظهرت صفوف بتواريخ مستقبلية كاملة (توصية probe-hidden-api-or-html-table تعني: ابحث عن JSON داخلي أولًا).
2. جامع متحفظ في `collect-source-candidates.mjs` (نفس نمط الجوامع العشرين): snapshot دليل + مفتاح dedupe + تواريخ ISO بتوقيت الرياض + `audiences[]`.
3. مصدر جديد في `data/source_registry.json` بتصنيف صريح: official / partner / discovery-only / evidence-monitor / extractor-backlog. **لا إضافة مصدر بلا تصنيف ودليل probe.**
4. كل extractor يمر عبر: probe → collect → auto-publish gate → validate → build → health-gate.

### B.3 بوابة القبول (Gate B) — على دفعتين
**B-1 (المصادر 1–5):**
- [ ] 5 جامعات نشطة جديدة (المجموع ≥ 25) وصفر collector_errors.
- [ ] الكتالوج ≥ 120 فعالية، ونصيب الفعاليات اللحظية (`moment`) ≥ 55%.
- [ ] تغطية ≥ 12 مدينة (تشمل جدة ومكة والمدينة وأبها).
**B-2 (المصادر 6–10):**
- [ ] المجموع ≥ 30 جامعًا نشطًا، الكتالوج ≥ 250 فعالية.
- [ ] كل فئة جمهور في §2 لديها ≥ 8 فعاليات قادمة حقيقية.
- [ ] تقرير الصحة يعرض المصادر الجديدة productive (وليس open_idle).

---

## §4 — المرحلة C: عمق "الرفيق الحي" (Live Companion) — **الأولوية 3**

**لماذا**: هذا هو الخندق التنافسي. webook تبيع تذاكر؛ نحن نرافق الحاضر لحظة بلحظة. حاليًا جدول حي واحد فقط.

### C.1 جداول الجلسات (Sessions) آليًا
1. وسّع extractors المصادر التي تنشر أجندة (Ithra, LEAP, مؤتمرات الجامعات, المعارض): التقط الجلسات (عنوان/وقت/قاعة/متحدث) إلى `sessions[]` في المرشح.
2. بوابة تفعيل آلية: فعالية بمصدر رسمي + ≥ 3 جلسات مكتملة الوقت ⇒ `live_schedule_ready: true` تلقائيًا (اليوم القرار يدوي — حوّله لقاعدة).
3. صفحة الفعالية تعرض: "يجري الآن / التالي بعد X دقيقة / القاعة" من `sessions[]` (المنطق موجود في event.html للفعالية التجريبية — عمّمه لكل فعالية لديها جلسات).

### C.2 يوم الفعالية
- عدّاد تنازلي + شريط تقدم (موجود) + **إشعار تغيير** من live-updates متصل بكل فعالية.
- زر "أنا هنا": يحفظ الفعالية ويثبتها في أعلى `today.html` (localStorage — لا حسابات).
- روابط اتجاهات Google Maps + **إحداثيات دقيقة** حيث يوفرها المصدر (حقل `lat/lng` اختياري في الschema).
- وضع الشاشة الكبيرة `screen.html` يقرأ فعاليتك المثبتة.

### C.3 التقويم كقناة رجوع
- ICS لكل فعالية (موجود) + **روابط اشتراك دائمة**: `feeds/city-<slug>.ics`، `feeds/for-<slug>.ics`، `feeds/all.ics` تتجدد مع كل build — المستخدم يشترك مرة ويصله كل جديد في تقويمه.
- أزرار "أضف للتقويم" (Google/Apple/ICS) على كل بطاقة.

### C.4 بوابة القبول (Gate C)
- [ ] `live_schedule_ready` ≥ 10 فعاليات، جميعها بجلسات حقيقية من مصادر رسمية.
- [ ] ملفات ICS الاشتراكية تُبنى لكل مدينة وفئة وتتحقق منها بوابة (`test:feeds` جديد: صحة صيغة ICS + VEVENT لكل فعالية قادمة).
- [ ] صفحة فعالية بجلسات تعرض "الآن/التالي" الصحيح (اختبار زمني ثابت الآن كما في test:event-kind).
- [ ] "أنا هنا" يثبّت الفعالية في today.html (فحص يدوي + لقطة شاشة في `output/`).

---

## §5 — المرحلة D: البحث والاكتشاف الزمني — **الأولوية 4**

### D.1 بحث عربي فوري (client-side)
1. عند البناء، ولّد `dist/search-index.json`: id، عنوان، عنوان مطبَّع (بلا همزات/تشكيل/ال)، مدينة، فئات، تواريخ، منظم.
2. صندوق بحث في الهيدر (كل الصفحات): يطابق على النص المطبَّع، النتائج فورية بدون مكتبات خارجية (≤ 30 سطر JS).
3. التطبيع في `scripts/arabic-normalize.mjs` (يُشارك مع مصنّف الجمهور).

### D.2 صفحات زمنية (أهم صفحات SEO)
- `today-events.html` (فعاليات اليوم) — غير صفحة `today.html` (وضع الحضور).
- `this-week.html`، `weekend.html` (الخميس-السبت)، `this-month.html`.
- تُبنى آليًا من الكتالوج مع عناوين وأوصاف عربية غنية وJSON-LD.

### D.3 SEO مرجعي
- JSON-LD `Event` مكتمل لكل صفحة فعالية (location بإحداثيات، offers إن وجد رابط تذاكر، organizer، eventStatus يعكس ongoing/live).
- عناوين بنمط "فعاليات {المدينة} {الشهر} {السنة} | EventLive".
- `sitemap.xml` يشمل صفحات الفئات والزمن (موجود جزئيًا — وسّعه).
- OG image مولَّدة لكل فعالية عند البناء (SVG→PNG بقالب موحد: اسم الفعالية + التاريخ + المدينة + شعار EventLive) — المشاركة في واتساب/X تظهر بطاقة غنية.

### D.4 بوابة القبول (Gate D)
- [ ] البحث يجد "الذكاء الاصطناعي" و"الذكاء الأصطناعي" و"ذكاء اصطناعي" بنفس النتائج (اختبار `test:search`).
- [ ] الصفحات الزمنية الأربع تُبنى وتتحدث يوميًا مع المزامنة.
- [ ] Rich Results Test يمر لعينة 5 صفحات فعالية (توثيق النتيجة في `reports/`).
- [ ] OG images تُولَّد لكل فعالية جديدة آليًا.

---

## §6 — المرحلة E: الثقة والجودة — **الأولوية 5**

1. **dedupe عابر للمصادر**: مفتاح تطبيع (عنوان مطبَّع + نافذة تاريخ ±1 يوم + مدينة) يلتقط Oxford ×2 وFamily Office ×3 في المحجوبات؛ المكررات تُدمج في مرشح واحد بمصادر متعددة (`sources[]`) — تعدد المصادر **يرفع** الثقة.
2. **فحص روابط دوري**: خطوة CI أسبوعية تفحص source_url/evidence_url لكل فعالية قادمة؛ الرابط المكسور ⇒ علم `evidence_broken` في تقرير الصحة (لا حذف آلي للفعالية).
3. **شارة الطزاجة**: "تم التحقق قبل N ساعة" على كل صفحة فعالية من `updated_at`.
4. **صفحة منهجية علنية** `methodology.html`: كيف نجمع، ما مصادرنا، لماذا نحجب مصادر الاكتشاف — هذه المرجعية تُبنى على الشفافية.
5. **بوابة أرشفة**: الفعالية المنتهية منذ > 30 يومًا تنتقل من الفهارس الرئيسية إلى `archive/` (تبقى للـ SEO والدليل).

**Gate E**: صفر مكررات في blocked report للعينة الحالية؛ `test:dedupe` يحمي المفتاح؛ فحص الروابط يعمل في CI مع تقرير؛ الأرشفة تعمل ولا تكسر روابط قديمة (redirect stub).

---

## §7 — المرحلة F: القياس والنمو — **الأولوية 6**

1. **Analytics محترم للخصوصية**: GoatCounter أو Plausible (script واحد، بلا كوكيز) — يقيس: زيارات الصفحات، مدن الزوار، أكثر الفعاليات فتحًا، استخدام "أنا هنا"، تحميلات ICS.
2. **مؤشر النجم الشمالي**: "مستخدم أسبوعي فعّال وقت الفعالية" = فتح today.html أو صفحة فعالية live في يومها.
3. لوحة KPI في `live-ops.html` (موجودة — أضف مقاييس المنتج بجانب مقاييس المصادر).
4. **RSS/JSON Feed** عام + لكل مدينة/فئة (`feeds/*.xml`) — يغذي النشرات والبوتات ومجمعات الأخبار.
5. زر مشاركة واتساب/X بنص عربي جاهز على كل بطاقة (موجود جزئيًا في share kit — عمّمه على البطاقات).

**Gate F**: analytics يعمل على الإنتاج بلا تدهور أداء (Lighthouse performance ≥ 90 يبقى)؛ feeds صالحة (validator)؛ تقرير KPI أسبوعي يتولد آليًا في `reports/`.

---

## §8 — المرحلة G: التوطين الثنائي (لاحقة — بعد Gate D)

- مرآة إنجليزية `/en/` للصفحات الرئيسية والفعاليات الكبرى (الجمهور غير الناطق بالعربية في المواسم والمؤتمرات الدولية كبير).
- `hreflang` بين النسختين. الترجمة من حقول المصدر الإنجليزية حيث توفرت (Misk/Ithra ينشرون ثنائيًا أصلًا) — لا ترجمة آلية رديئة.

**Gate G**: 20 أهم فعالية متاحة بالإنجليزية؛ hreflang صحيح؛ لا تكرار محتوى في Search Console.

---

## §9 — المرحلة H: الشراكات والتموضع الرسمي (مسار موازٍ — قرارات مالك)

هذه ليست مهام كود؛ Codex يجهّز **الأصول** والمالك يقود التواصل:
1. **NEC / Saudi Events**: الهدف الاستراتيجي الأعلى — feed رسمي أو اعتماد. جهّز one-pager عربي (من نحن، أرقام التغطية، ماذا نطلب).
2. **webook / Hala Yalla**: شراكة روابط تذاكر (نحن اكتشاف + مرافقة، هم البيع) — عمولة affiliate لاحقًا.
3. **الجهات المنظمة مباشرة**: صفحة `organizers.html` (موجودة) + مسار "فعّل جدولك الحي" بالبريد (موجود) — أضف نموذج CSV/قالب جاهز للمنظمين.
4. لا API خاص ولا اتفاقية تُوقَّع بلا المالك.

---

## §10 — ترتيب التنفيذ والاعتماديات

```
A (فئات الجمهور)  ──┐
                    ├──> B-1 (extractors 1–5) ──> C (الرفيق الحي) ──> B-2 (extractors 6–10)
                    │                                   │
                    └──────────> D (بحث + زمن + SEO) <──┘
                                        │
                          E (ثقة/جودة) ─┴─ F (قياس/نمو) ──> G (إنجليزي)
H (شراكات) يسير موازيًا من اليوم الأول (أصول فقط)
```
- **ابدأ بـ A ثم B-1**: الفئات أولًا حتى يولد كل extractor جديد فعاليات مصنفة من المنبع.
- C قبل B-2: عمق المنتج قبل مزيد من العرض.
- E وF يمكن تشغيلهما بالتوازي بعد D.

## §11 — سجل اللاحق (يتطلب backend/قرارات — لا يُنفذ الآن)
- إشعارات Push حقيقية (تتطلب خدمة) — البديل الحالي: اشتراك ICS + PWA.
- حسابات مستخدمين ومزامنة عبر الأجهزة.
- تطبيق جوال أصلي (PWA الحالي يغطي المرحلة).
- بيع تذاكر/عمولات (بعد شراكة webook).
- لوحة إدخال للمنظمين self-serve (تتطلب استضافة نماذج).

## §12 — تعريف "المرجع الأول" (متى نعلن النجاح)
1. تغطية: ≥ 500 فعالية نشطة، ≥ 20 مدينة، كل فئة جمهور ≥ 15 فعالية قادمة.
2. طزاجة: كل فعالية تحققنا منها خلال ≤ 24 ساعة.
3. عمق: ≥ 50 جدولًا حيًا مفعّلًا.
4. وصول: صدارة نتائج بحث Google لـ"فعاليات الرياض اليوم" وأخواتها.
5. ثقة: صفر فعاليات وهمية منشورة؛ منهجية علنية؛ مصدر رسمي لكل صف.

---

## ملحق: قائمة فحص لكل Pull Request من Codex
- [ ] أوامر التحقق القياسية (§0) كلها PASS.
- [ ] ميزة جديدة ⇒ اختبار انحدار جديد + سكربت npm موثق في `scripts/README.md`.
- [ ] لا كسر لأي رابط قائم في dist (فحص sitemap قبل/بعد).
- [ ] التقارير الإدارية (`reports/`, `dist/source-health.html`) تعكس أي مصدر/حقل جديد.
- [ ] لا مصدر جديد بلا تصنيف صريح ودليل probe في السجل.
- [ ] لقطات شاشة قبل/بعد لأي تغيير واجهة في `output/screenshots/`.

---
---

# §13 — سجل الحالة + السبرنت متعدد المسارات B1.2 (توجيه التنفيذ الحالي)

> أُضيف 2026-07-05 بعد تدقيق مستقل لنتائج الجولة الأولى. **هذا القسم هو التوجيه النافذ الآن** — نفّذ مساراته الخمسة كلها في هذه الجولة، ثم سلّم تقريرًا واحدًا شاملًا (§13.7).

## §13.0 سجل الحالة (مدقَّق 2026-07-05)

| البند | الحالة |
|---|---|
| المرحلة A (فئات الجمهور) | ✅ PASS — 10 فئات مفعّلة على الكتالوج، test:audience موجود |
| B-1 | ⚠️ جزئي — 24 جامعًا نشطًا لكن **11 منتجًا فقط و13 يرجع صفرًا** |
| الكتالوج | 54 (المطلوب ≥120) — moment 64% ✅ |
| المدن | 8 (المطلوب ≥12)، منها 28 رياض و7 "Saudi Arabia" placeholder |
| أخطاء الجلب | 0 ✅ · published_new آخر دورة: 9 ✅ |

**التشخيص الحاكم**: الجوامع الصفرية الـ13 كلها تفشل بنفس النمط: «No future date-complete candidates found by the conservative extractor». المشكلة في **قراءة التواريخ والمحتوى المتأخر التحميل**، لا في المصادر. رفع مردود الموجود أثمن من إضافة مصادر جديدة.

الجوامع الصفرية الـ13: `ithra-events, moc-cultural-calendar, moc-cultural-subportals, mos-events, sdaia-academy-programs, sdaia-calendar-events, saudi-pro-league-fixtures, mdlbeast-events, monshaat-events, experience-alula-events, misk-hub-events, jcci-events-center, code-mcit-programs`

---

## §13.1 المسار 1 — محرك المردود (Yield Engine) — أعلى أولوية

**الهدف**: من 11 منتجًا إلى ≥ 18، ومن 54 فعالية إلى ≥ 120.

1. **أداة تشخيص أولًا** — `scripts/source-yield-report.mjs` (+ سكربت npm `sources:yield`):
   لكل جامع نشط: حجم الجلب، الصفوف المكتشفة في الصفحة، المسقَط منها **مع سبب الإسقاط** (لا تاريخ / تاريخ ماضٍ / حقول ناقصة / dedupe / خطأ تحويل تاريخ)، والمكتوب فعليًا. يُدمج في `sources:sync` وتقرير الصحة. بدون هذه الأداة أنت تصلح أعمى.
2. **وحدة تواريخ مشتركة** — `scripts/date-parse-utils.mjs`:
   أشهر عربية (يناير/كانون…) وإنجليزية، مدايات ("10–12 يوليو"، "من 5 إلى 8"، "Jul 10-12")، سنوات ضمنية (أقرب مستقبل)، توقيت الرياض افتراضًا. + اختبار `test:date-parse` بعينات حقيقية من snapshots المصادر الفاشلة.
3. **أصلح الـ13 الصفرية واحدًا واحدًا** بهذا البروتوكول:
   افتح آخر snapshot في `data/raw/source-snapshots/` للمصدر ← إن كانت الصفحة client-rendered ابحث داخل الـHTML عن JSON مضمّن (`__NEXT_DATA__`, `window.__NUXT__`, `application/ld+json`, endpoints في `<script>`) واستخرج منه ← إن وجدت API داخلي عام بلا مفاتيح استخدمه بترويسة User-Agent واضحة ← حدّث المستخرج والتواريخ عبر الوحدة المشتركة.
4. **عمّق المنتجين** (المحصورين في الصفحة الأولى): pagination/أشهر متعددة بسقف متحفظ (≤ 5 صفحات أو 90 يومًا قادمة).
5. **أهداف مردود لكل مصدر** (قِس بها نجاحك):
   Visit Saudi Calendar ≥ 40 · SPL fixtures ≥ 30 (جولات كاملة بمدنها) · Ithra ≥ 12 · MOC ≥ 10 · AlUla ≥ 8 · كل من SDAIA/CODE/Misk-events/Monsha'at/MDLBEAST/JCCI ≥ 5.
   مصدر تعذّر إصلاحه بعد محاولتين موثقتين: سجّل السبب في yield report وانتقل — لا تحفر بلا نهاية.

## §13.2 المسار 2 — حقيقة المدن (City Truth)

**الهدف**: ≥ 12 مدينة حقيقية، placeholder ≤ 5%.

1. `scripts/city-utils.mjs`: قائمة مدن معيارية (عربي/إنجليزي/تهجئات: Riyadh/الرياض، Jeddah/جدة/Jiddah، Makkah/Mecca، المدينة/Madinah، الخبر/الدمام/الظهران، أبها/خميس مشيط، العلا، بريدة/القصيم، الطائف، تبوك، حائل، جازان، نجران، ينبع…) + استخراج المدينة من العنوان/المكان/صفحة التفاصيل.
2. "Saudi Arabia" تبقى فقط للوطني الحقيقي أو Online — راجع الصفوف السبعة الحالية وصحّح ما أمكن من مصدره.
3. صفحات المدن تتولد آليًا لكل مدينة جديدة (النمط قائم) + اختبار `test:city` (تطبيع التهجئات).
4. SPL وVisit Saudi هما أكبر رافعتي مدن — اربط المسار 1 بهذا الهدف.

## §13.3 المسار 3 — بداية الرفيق الحي (سحب مبكر من C)

**الهدف**: `live_schedule_ready` من 1 إلى ≥ 5 بجداول حقيقية.

1. حقل `sessions[]` في schema المرشح والكتالوج (عنوان/بداية/نهاية/قاعة اختياري/متحدث اختياري).
2. التقط الجلسات حيث ينشرها المصدر (إثراء والمعارض والمؤتمرات غالبًا تنشر أجندة في صفحة التفاصيل).
3. **قاعدة تفعيل آلية**: مصدر رسمي + ≥ 3 جلسات مكتملة الوقت ⇒ `live_schedule_ready: true` تلقائيًا (اليوم قرار يدوي — حوّله قاعدة مُختبرة `test:live-ready`).
4. صفحة الفعالية تعرض "يجري الآن / التالي" من sessions (عمّم منطق event.html الحالي).

## §13.4 المسار 4 — الاكتشاف الزمني والبحث (سحب مبكر من D — مستقل تمامًا عن البيانات)

1. صفحات زمنية تتولد مع كل build: `today-events.html`، `this-week.html`، `weekend.html` (خميس-سبت بتوقيت الرياض) — بطاقات + عدّادات + JSON-LD + روابط في الهيدر وsitemap.
2. `dist/search-index.json` عند البناء + صندوق بحث فوري في الهيدر (تطبيع عربي: همزات/تاء مربوطة/ال/تشكيل) — بلا مكتبات خارجية. الوحدة المشتركة `scripts/arabic-normalize.mjs` يستعملها البحث ومصنّف الجمهور معًا.
3. اختبار `test:search`: "الذكاء الاصطناعي" = "ذكاء أصطناعي" = "الذكاء الأصطناعي".

## §13.5 المسار 5 — الثقة والتصرف في المحجوبات

1. **dedupe عابر للمصادر**: مفتاح (عنوان مطبَّع + مدينة + نافذة ±1 يوم) يدمج المكررات في مرشح واحد بمصفوفة `sources[]` — تعدد المصادر يرفع الثقة لا يكررها. اختبار `test:dedupe` بعينة Oxford ×2 وFamily Office ×3 الحالية.
2. **العمالقة المحجوبون** — GEA (403) وRiyadh Season (bot-protection) وriyadh-city (fetch failed): جرّب المسارات المشروعة فقط (sitemap.xml، RSS، JSON عام، صفحات AMP). **ممنوع الالتفاف على أي حماية** (§0.8). إن بقيت محجوبة: أعد تصنيفها `partnership` بملاحظة واضحة وانتقل — لا تستهلك الجولة فيها.

## §13.6 قواعد التوازي في هذه الجولة

- المساران 1 و2 يلمسان نفس الملفات (الجوامع) — نفّذهما معًا كوحدة واحدة (أصلح المستخرج والتاريخ والمدينة في نفس المرور على كل مصدر).
- المساران 3 و4 مستقلان — نفّذهما بأي ترتيب بعد أو بالتوازي مع 1+2.
- المسار 5 خفيف — آخر الجولة.
- بعد **كل مصدر تصلحه**: `sources:collect` للمصدر ثم السلسلة القياسية (§0). لا تراكم إصلاحات غير مختبرة.

## §13.7 بوابة الجولة (Gate B1.2) + التقرير المطلوب

**البوابة — كلها PASS أو الجولة لم تكتمل:**
- [ ] الكتالوج ≥ 120 و moment ≥ 55%
- [ ] جوامع منتجة ≥ 18 من 24 (صفرية ≤ 6، وكل صفري متبقٍ له سبب موثق في yield report)
- [ ] مدن ≥ 12 و placeholder "Saudi Arabia" ≤ 5%
- [ ] `live_schedule_ready` ≥ 5 بجلسات حقيقية
- [ ] الصفحات الزمنية الثلاث + البحث يعملان (test:search PASS)
- [ ] صفر collector_errors · كل الاختبارات القديمة والجديدة PASS (بما فيها test:event-kind وtest:audience)
- [ ] الاختبارات الجديدة السبعة موجودة وخضراء: `test:date-parse, test:city, test:live-ready, test:search, test:dedupe` + سكربت `sources:yield` يعمل داخل sync

**التقرير الواحد** — `reports/sprint-b12-report.md`:
جدول قبل/بعد لكل مؤشر أعلاه · جدول مردود لكل مصدر من الـ24 (قبل/بعد/سبب البقاء صفريًا إن وجد) · قائمة المدن الجديدة · الفعاليات المفعَّل جدولها الحي · لقطات شاشة (رئيسية/اليوم/بحث/صفحة زمنية) في `output/screenshots/` · جدول البوابة PASS/FAIL. **هذا التقرير هو ما يعود للمالك — اجعله كاملًا حتى لا تحتاج جولة أسئلة.**

---

# §14 — جولة B1.3: جراحة المردود + عرض المدن (التوجيه النافذ)

> أُضيف 2026-07-05 بعد جولة B1.2. البنية الآن قوية (yield diagnostic + 5 اختبارات جديدة + صفحات زمنية + بحث) لكن أرقام البوابة لم تتحرك: 66/120، منتجون 11، مدن 8، لايف 1. تقرير المردود كشف سبب موت كل مصدر — **هذه الجولة جراحة موجهة بحسب سبب الموت، لا بروتوكول عام.**

## §14.0 خريطة الأسباب (من reports/sprint-b12-report.md — لا تعد تشخيصها، عالجها)

| سبب الموت | المصادر | العلاج المقرر |
|---|---|---|
| `past-date` (22 صفًا!) | Monsha'at (12)، Misk Events (5)، MoS (3)، AlUla (1)، JCCI (1) | §14.1-أ |
| `no rows detected` | MOC Calendar، CODE MCIT | §14.1-ب |
| `signals exist, no complete rows` | SDAIA ×2، MDLBEAST، MOC Commissions | §14.1-ب |
| detail fetch timeout | Ithra | §14.1-ج |
| API يرجع موسمًا منتهيًا | SPL | §14.1-د |

## §14.1 المسار 1 — جراحة الأسباب

**أ) خلل past-date (أعلى مردود متوقع — افحصه أولًا):**
1. وسّع `sources:yield` ليطبع **عينات الصفوف المسقطة**: النص الخام للتاريخ + ناتج التحويل + سبب الإسقاط (أول 5 عينات لكل مصدر). هذا يفصل فورًا بين فرضيتين:
   - **خطأ استنتاج السنة الضمنية**: "15 مارس" بلا سنة تُحل إلى 2026-03 الماضي بدل 2027-03. القاعدة الصحيحة: التاريخ بلا سنة يُحل لأقرب وقوع **مستقبلي** (اليوم − 1). أصلح في `date-parse-utils` + حالات في `test:date-parse`.
   - **صفحة أرشيف**: المصدر يعرض الماضي في landing والقادم في مسار آخر (`?filter=upcoming`، تبويب، صفحة 2). افتح snapshot المصدر وابحث عن رابط "القادم/Upcoming" واجعل الجامع يجلبه.
2. بعد الإصلاح، الهدف: Monsha'at ≥ 8 · Misk Events ≥ 4 · MoS ≥ 3 · JCCI ≥ 3 · AlUla ≥ 6.

**ب) الصفحات client-rendered (MOC ×2، CODE، SDAIA ×2، MDLBEAST):**
ابنِ مساعدًا مشتركًا `scripts/embedded-json-utils.mjs`: يبحث في HTML عن `__NEXT_DATA__` / `window.__NUXT__` / `<script type="application/ld+json">` / مصفوفات JSON داخل `<script>` تحوي مفاتيح تواريخ، ويرجعها مفكوكة. استخدمه في هذه الجوامع الستة. إن كشف الـHTML عن endpoint داخلي عام (fetch/axios URL في الكود) استعمله مباشرة بترويسة UA واضحة. الهدف: ≥ 4 من الستة تصبح منتجة بـ≥ 5 لكل منها.

**ج) إثراء**: جلب صفحات التفاصيل بمهلة أطول (15s) + إعادة محاولة واحدة + تسلسل لا توازٍ + سقف 20 صفحة تفاصيل لكل دورة. إثراء ينشر مواعيد عروض حقيقية — الهدف ≥ 10 فعاليات **بجلسات** (يخدم المسار 3 مباشرة).

**د) الدوري**: جرّب باراميتر الموسم في الـAPI (2026-27 / seasonId التالي). إن لم يُنشر الموسم الجديد بعد: وثّق في الجامع تعليقًا `seasonal: fixtures typically publish in August` واتركه نشطًا — سيمتلئ وحده. **لا يُحسب ضد البوابة** (استثناء موسمي موثق).

## §14.2 المسار 2 — سبعة مصادر جديدة تفتح المدن (بترتيب الفحص)

القاعدة: `sources:probe` أولًا، ابنِ فقط عند صفوف مستقبلية مكتملة. سجّل كل مصدر في registry بتصنيفه ودليله.

| # | مصدر | مدينة/قيمة | ملاحظة probe |
|---|---|---|---|
| 1 | SFDA Events | رياض + وطني | probe السابق قال **build-html-detail-extractor** — جاهز |
| 2 | Saudi Space Agency | رياض/وطني، فئة tech | probe قال inspect-client-api — استعمل §14.1-ب |
| 3 | Jeddah Events / أمانة جدة أو Jeddah Season | **جدة** — أكبر فجوة مدن | ابحث عن التقويم الرسمي وprobe |
| 4 | غرفة الشرقية (Asharqia Chamber) | **الدمام/الخبر** | فعاليات أعمال وتوظيف |
| 5 | جامعة الملك عبدالعزيز أو KAUST events | **جدة/ثول** — طلاب وباحثون | صفحات أجندة جامعية عادة نظيفة |
| 6 | Soudah Development / فعاليات عسير الموسمية | **أبها/السودة** | يقوّي discover-aseer الهزيل (1) |
| 7 | Diriyah Company / بوابة الدرعية | **الدرعية** | لديها فعلًا فعالية واحدة — مصدر مباشر أفضل |
| احتياط | غرفة القصيم (بريدة) · Hail Season · Taif Season · جامعة الملك سعود | قصيم/حائل/طائف | إن فشل probe لأي من السبعة |

الهدف: ≥ 5 من السبعة نشطة منتجة (≥ 4 لكل منها)، والمدن الإجمالية **≥ 12**.

## §14.3 المسار 3 — جلسات حقيقية ولايف ≥ 5

1. **إثراء** (بعد §14.1-ج): مواعيد العروض = جلسات حقيقية.
2. **المعارض** (Dhahran Expo 15 فعالية، RFECC 6): ساعات الافتتاح اليومية المنشورة = جلسات من نوع جديد `session_type: "opening-hours"` (صادقة ومفيدة للحاضر: "المعرض مفتوح اليوم 4م–11م"). حدّث قاعدة live-ready لتقبل ≥ 3 جلسات من أي نوع، وصفحة الفعالية تعرضها بتسمية "ساعات الزيارة".
3. أي مؤتمر بأجندة منشورة في المصادر الجديدة (SFDA غالبًا ينشر أجندة ورش).
الهدف: `live_schedule_ready` **≥ 5** (منها ≥ 2 بجلسات برنامج فعلية لا ساعات فقط) + تحديث `test:live-ready` للنوع الجديد.

## §14.4 المسار 4 — قنوات الرجوع (مستقل، من C.3/F)

1. **ملفات اشتراك ICS دائمة** تُبنى مع كل build: `dist/feeds/all.ics` + `feeds/city-<slug>.ics` لكل مدينة + `feeds/for-<slug>.ics` لكل فئة جمهور — VEVENT لكل فعالية قادمة، والمشترك يصله الجديد تلقائيًا في تقويمه.
2. روابط "اشترك بتقويم مدينتك/فئتك" في صفحات المدن والفئات والرئيسية.
3. زر مشاركة واتساب + X بنص عربي جاهز (عنوان + تاريخ + رابط) على كل بطاقة فعالية.
4. اختبار `test:feeds`: صحة صيغة ICS، كل فعالية قادمة ممثلة، الترميز العربي سليم.

## §14.5 بوابة الجولة (Gate B1.3) والتقرير

- [ ] الكتالوج ≥ 110 (موثوق فقط) و moment ≥ 55%
- [ ] منتجون ≥ 18 (من ~31 جامعًا بعد الإضافات) · صفريون ≤ 8 وكل صفري له سبب/استثناء موثق (SPL الموسمي لا يُحسب)
- [ ] مدن ≥ 12
- [ ] live_schedule_ready ≥ 5 (≥ 2 منها جلسات برنامج)
- [ ] feeds تُبنى وtest:feeds أخضر · أزرار المشاركة على البطاقات
- [ ] عينات الإسقاط ظاهرة في sources:yield · صفر collector_errors · كل الاختبارات (القديمة + الجديدة) PASS
- [ ] التقرير: `reports/sprint-b13-report.md` بنفس صيغة B1.2 (قبل/بعد لكل مصدر، المدن، اللايف، لقطات شاشة، جدول البوابة) + **سطر واحد لكل مصدر فشل: ماذا جربت ولماذا استُبعد**

قاعدة الجولة: أرقام البوابة الإجمالية هي الحكم. مصدر عنيد بعد محاولتين موثقتين → سجّل وانتقل. لا تلمس GEA/موسم الرياض إلا بمسار مشروع.
