# دستور البوابات — Gate Governance

سجل البوابات (90+ فحصًا في بطاريتَي source-sync وdeploy) هو خط الدفاع الذي يجعل
المنصة مستقرة بلا ترقيع يدوي: كل بوابة وُلدت من عطل حقيقي وقع، وتحظر **فئته**
لا حالته. العدد ليس مقياسًا — نوعية البوابة هي المقياس. القواعد الأربع:

## 1. كل بوابة تعلن صنفها

- **بنيوية حاجبة (blocking-structural):** تفشل فقط على انحدار كود/قالب.
  تعيش في `ci:site-gates` أو بطاريات الـworkflows وتُحمّر الخط.
- **محتوى مُبلّغة (report-only-content):** تتأثر بحالة الكتالوج القادمة من
  مصادر خارجية (ملخص ضعيف، وسم خاطئ). تعمل كل دورة لكن **غير حاجبة**
  (نمط `test:prelaunch-data-quality` في source-sync.yml) وتُبرز الدين في السجل.
- **تحضيرية يدوية (preflight-only):** تعدّل الكتالوج أو تعيد البناء داخل
  التشغيل (مثل `test:cover-content-freshness`) — تعيش في `launch:preflight`
  فقط ولا تدخل خط النشر أبدًا، لأن dist بعدها لا يصلح للنشر.

## 2. التوكيد يمسك هويات ثابتة، لا نصوص عرض

قانون 2026-08-01: العناوين والأماكن وأسماء المسارات حقول مسجلة في MT
(`CONTENT_PROSE_FIELDS`) وتتبدل لغتها في أي دورة. الاختبار يختار بـ`id`
أو `title_original` أو URL أو بنية (عدد مسارات مميزة)، ولا يطابق نص عرض
قابلًا للترجمة أبدًا. كذلك: التوكيد يجب أن يُمارَس في كل بناء — توكيد لا
يتفعل إلا في حالات نادرة هو لغم مؤجل (درس قسم «هذا الشهر»، أغسطس 2026).
وقوالب dist الملتزمة تمتص بيانات «الفعالية الحالية» المتغيرة كل دورة —
أي مصنِّف يعامل نص القوالب كـ«كروم ثابت» يجب أن يستثني ما يطابق كتالوج
الفعاليات (درس الانحدارات الـ15 الزائفة، دورة 30739850995).

## 3. لا تكرار لمصادر الحقيقة

ماشي الملفات والقوائم المشتركة تُستورد ولا تُنسخ:
`owner-only-pages.mjs`، `legacy-redirect-pages.mjs`، `event-priority.mjs`،
`duration-label.mjs`، `internal-prose-utils.mjs`. أي بوابة جديدة تحتاج
قائمة موجودة في بوابة أخرى = وحدة مشتركة جديدة، لا نسخة.

## 4. سقف زمني وواجب التوصيل

- بوابة خارج كل البطاريات = تتعفن صمتًا (ثبت 4 مرات في يوم واحد). كل بوابة
  جديدة تُوصَل يوم ولادتها في `ci:site-gates` أو بطارية workflow أو تُعلَّم
  preflight-only صراحةً مع السبب.
- بطارية `ci:site-gates` تبقى تحت ~3 دقائق. اختبارات المتصفح (Playwright)
  تحتاج مبررًا؛ الافتراضي سكربتات Node لحظية.
- أسماء خطوات الـworkflows بلا نقطتين `:` غير مقتبسة (كسر YAML أوقف
  الجدولة كاملة، 2026-08-01).

## 5. مصدر واحد لبوابات النشر — لا انحراف بين المسارات

قانون 2026-08-02: كانت `deploy.yml` (خط الإنتاج الرسمي، "EventLive MVP
Pipeline") و`source-sync.yml` (الخط الذي يُحدّث الموقع الحي فعليًا كل ٦
ساعات) تحملان كل واحدة قائمة أوامر `npm run` مكتوبة يدويًا ومنفصلة. القوائم
انحرفت عن بعضها بصمت: ٤٠ فحصًا (تدقيقات الجودة والتيسير — `audit:axe`،
`audit:lighthouse`، ...) كانت تعمل في `deploy.yml` فقط ولا تعمل في
`source-sync.yml` إطلاقًا. حصلت ثلاث فجوات مستقلة وقعت معًا:

1. لا تحقق قبل الدمج (لا `pull_request` على أي workflow قبل هذا القانون).
2. `deploy.yml` — الخط الوحيد الذي يشغّل بوابات الجودة — لا يستطيع إيقاف أي
   شيء، لأنه ليس الخط الذي ينشر.
3. `source-sync.yml` — الخط الذي ينشر فعلًا — لا يشغّل تلك البوابات.

نتيجة الفجوات الثلاث معًا: انحدار وصول حرج (`aria-required-children` على
`.cal-strip`) وصل الإنتاج وبقي حيًا ٥ أيام (٢٠٢٦-٠٧-٢٨ فصاعدًا) وكل إشارة
مرئية كانت خضراء. القواعد الآن:

- **مصدر واحد:** بطارية `ci:publish-quality-gates` (`scripts/run-publish-quality-gates.mjs`)
  هي القائمة الوحيدة لفحوصات جودة النشر. أي مسار جديد أو قديم يستدعيها، لا
  ينسخها.
- **`deploy.yml` حاجبة:** فشل أي فحص في البطارية يوقف الخط (لا تغيير عن
  السابق — هو الخط الرسمي المُبوّب).
- **`source-sync.yml` لا تحجب النشر، لكن التشغيل ذاته ينتهي أحمر:**
  `continue-on-error: true` على خطوة البطارية يمنع التجميد فقط — النشر
  (`Deploy to GitHub Pages`) يمضي دون توقف (قسم ٤ من هذا الدستور ينطبق
  هنا حرفيًا: هذا المشروع نجا من انقطاع مزامنة استمر ٨ أيام مرة واحدة
  بالفعل). **تصحيح 2026-08-02:** لا يكفي أن يتحول الفشل إلى `::error::`
  وقسم في `$GITHUB_STEP_SUMMARY` فقط — تشغيل أخضر بملاحظة داخلية لا يراه
  أحد إلا من فتح التشغيل أصلًا، وهذا بالضبط ما فشل فيه الخط الأصلي. لذلك
  خطوة أخيرة (بعد النشر وبعد فحص صحة الخط الرسمي) تفشل **التشغيل نفسه**
  (`exit 1`) إن كانت نتيجة خطوة البطارية غير ناجحة — بعد أن يكون النشر قد
  تم فعلًا، فلا ينكسر قسم ٤. رسالة الفشل تقول صراحة إن الموقع نُشر وإن هذا
  انحدار جودة، لا عطل نشر.
- **فحص صحة الخط الرسمي يبقى أخضر عمدًا:** ما يُحمّر هذا التشغيل هو "الموقع
  الذي نُشر للتو فيه انحدار جودة" — لا "workflow آخر غير راضٍ". `deploy.yml`
  الأحمر مرئي بالفعل في تشغيله الخاص؛ تحمير `source-sync.yml` أيضًا بسببه
  يخلق حلقة حمراء دائمة وإرهاق تنبيه (نفس السبب الذي أفقد الرؤية أصلًا). لذلك
  `scripts/production-pipeline-health-check.mjs` يبقى `::error::` + قسم
  ملخص + دليل JSON فقط، ولا يُخرج بكود غير صفري أبدًا.
- **تحقق قبل الدمج:** `pr-verify.yml` يشغّل نفس البطارية (`build` ثم
  `ci:publish-quality-gates`) على كل `pull_request` — بلا خطوات Pages وبلا
  صلاحيات كتابة. هذا يسد الفجوة رقم ١ أعلاه. الفحص مرئي على كل PR لكنه ليس
  required status check — ذلك إعداد حماية الفرع، قرار المالك لا القانون.
- **حارس الانحراف:** `scripts/publish-gate-drift-regression-test.mjs`
  (`npm run test:publish-gate-drift`, داخل `ci:site-gates`) يفشل إن: (أ) أي
  workflow يستخدم `actions/deploy-pages@` ولا يستدعي البطارية المشتركة (إلا
  إن كان مُدرجًا صراحة كاستثناء موثّق — `pages.yml` وحدها اليوم؛ نشر يدوي
  `workflow_dispatch` احتياطي **متعمّد**، لا مجرد موثّق: هو المسار اليدوي
  الوحيد المتاح إن تعطّل الخط المُبوّب *نفسه* (لا فحص داخله فقط، بل ملف
  الـworkflow ذاته) — إخضاعه لنفس البطارية يُلغي شبكة الأمان تلك، انظر رأس
  الملف نفسه)، (ب) فحص سقط من داخل البطارية نفسها، (ج) ترتيب audit-قبل-test
  انكسر، أو (د) `deploy.yml`/`source-sync.yml` فقدا صفة الحجب/عدم الحجب
  المطلوبة لكل منهما.
- **المُحلِّل المشترك (resolver):** `scripts/workflow-gate-resolver.mjs`
  يجيب على سؤال واحد فقط — "هل السكربت X يعمل فعليًا لو شغّلنا هذا
  الـworkflow؟" — ويقرأ `scripts/publish-quality-gates-list.mjs` (مصدر
  الحقيقة الوحيد لعناصر البطارية، يستورده أيضًا
  `scripts/run-publish-quality-gates.mjs`) ليتوسّع عبر طبقة واحدة من
  التفويض: استدعاء `npm run ci:publish-quality-gates` يُحسب كأنه استدعى كل
  عنصر بداخلها. **درس 2026-08-02:** `scripts/security-review-audit.mjs` كان
  يفحص نص `deploy.yml` بحثًا عن السلاسل الحرفية `audit:dependencies`/
  `audit:secret-env`/`audit:web-quality` كدليل أنها "موصولة في CI". حين
  انتقلت هذه السكربتات إلى البطارية المشتركة، اختفت السلاسل الحرفية من
  الملف فأبلغ التدقيق `SECURITY_REVIEW_FAIL findings=3` رغم أن الفحوصات
  الثلاثة تعمل بلا أي تغيير — عطل ذاتي الصنع بسبب هذا القانون نفسه، لا
  انحدار حقيقي. **القاعدة الآن:** أي بوابة تسأل "هل السكربت X موصول في
  CI؟" بفحص نص workflow يجب أن تمر عبر `workflowRunsScript()` من
  `scripts/workflow-gate-resolver.mjs`، لا بحثًا حرفيًا مباشرًا — وإلا
  فإنها عمياء عن أي تفويض عبر بطارية مشتركة. لا يجوز إطلاقًا إضعاف بوابة
  كهذه أو تحويلها إلى تحذير لإسكات فشل كاذب، ولا زرع السلسلة الحرفية في
  تعليق لخداع الفحص — بوابة يُرضيها تعليق أسوأ من انعدام البوابة.
- **رؤية صحة الخط الرسمي:** `scripts/production-pipeline-health-check.mjs`
  يعمل داخل `source-sync.yml` (غير حاجب دائمًا) ويقرأ آخر نتيجة لـ`deploy.yml`
  على `main` عبر `gh run list`؛ إن كانت فاشلة يصدر `::error::` وقسمًا في
  الملخص. هذا يسد فجوة أخرى: لم يكن هناك من يخبر أحدًا أن الخط الرسمي أحمر
  بينما النشر مستمر من مسار آخر.

## 6. بوابات إلزامية مقابل استشارية — لا أحد يستشهد باستشارية كدليل تحقق

قانون 2026-08-02: ليست كل خطوة في `deploy.yml` قادرة على إيقاف الخط رغم أن
قارئ الملف يفترض عكس ذلك بشكل معقول. صنفان:

- **إلزامية (enforcing):** تُنهي العملية برمز خروج غير صفري عند الفشل — إما
  `process.exit(1)` صريح أو استثناء غير مُمسوك من `node:assert` (النمط
  السائد في سكربتات `*-regression-test.mjs`). هذه فقط قادرة على حجب النشر.
- **استشارية (advisory):** تكتب تقريرًا وتخرج بكود صفر دائمًا مهما كانت
  النتيجة. ثلاث معروفة اليوم، وتحقّق منها بالقراءة المباشرة لا التخمين:
  - `audit:release-deploy-rollback` (`scripts/release-deploy-rollback-audit.mjs`):
    لا `process.exit` إطلاقًا. تقرن بـ`test:release-deploy-rollback` الذي
    *يملك* `process.exit(1)` لكنه يتحقق من شكل التقرير فقط (`schema`،
    `checks` المطلوبة) ويقبل الحالة `PARTIAL` كحالة صحيحة صراحة — أي أن
    الزوج كاملًا لا يمكنه أبدًا الحجب على "غير جاهز فعليًا"، فقط على تقرير
    مشوّه. ثلاثة من فحوصها السبعة تقرأ متغيرات `RELEASE_*_STATUS` التي لا
    تُملأ إلا داخل job منفصل (`verify-release`, بعد النشر) — فداخل `build`
    هي عاجزة بنيويًا عن أن تكون غير PARTIAL، وجعلها حاجبة يجمّد كل نشر
    (ينتهك قسم ٥).
  - `readiness:standard` (`scripts/delivery-readiness-standard-audit.mjs`):
    لا `process.exit`. `test:readiness-standard` يتحقق من عدد البوابات (٢٣)
    وشكل كل بوابة فقط، ويقبل `PARTIAL`/`NOT_STARTED`/`OWNER_RESERVED`/`N/A`
    كحالات صحيحة — لا يقرأ حقل `release_verdict` الإجمالي إطلاقًا (يمكن أن
    يكون `NOT_READY` والاختبار ينجح رغم ذلك). `NOT_READY`/`OWNER_RESERVED`
    جزء متعمّد من التصميم لبوابات محجوزة للمالك.
  - `owner:command-center` (`scripts/owner-command-center.mjs`): لا
    `process.exit`. `test:owner-command-center` يتحقق من وجود التقارير
    وschema وحقول بسيطة فقط، لا من حالة "جاهز" إجمالية.
- **القاعدة:** مخرجات أي بوابة استشارية يجب أن تظهر في `$GITHUB_STEP_SUMMARY`
  (خطوة "Advisory gates status" في `deploy.yml`) — ولا يجوز لأي تقرير أو
  شخص الاستشهاد ببوابة استشارية كدليل تحقق فعلي. إن احتجت حجب النشر على
  حالة `release_verdict`/الجاهزية الفعلية، أضف اختبارًا إلزاميًا جديدًا
  يقرأها صراحةً بدل افتراض أن الاختبار الحالي يفعل ذلك.
- حارس الانحراف يتحقق آليًا (grep-level: `process.exit(1)`/`process.exitCode = 1`
  أو استيراد `node:assert`) أن مجموعة السكربتات "الإلزامية" المعروفة لم
  تفقد قدرتها على الفشل بصمت — لا يتحقق من المعنى (شكل مقابل جوهر)، فقط من
  وجود مسار فشل حقيقي في الكود. هذا الفحص هش عمدًا وبسيط: فحص شكل جديد
  (مثل `readiness:standard`) يحتاج تحديث القائمة يدويًا، لا اكتشافًا تلقائيًا.

## 7. تقرير محفوظ في git ≠ حالة حالية

قانون 2026-08-02: `reports/axe-accessibility-audit.json` و
`reports/lighthouse-performance-audit.json` المحفوظان على `main` كانا
مؤرخين ٢٠٢٦-٠٧-١٠ — ثلاثة أسابيع قديمين — بينما التجمّد جعل CI لا يعيد
توليدهما أبدًا (لأن `audit:lighthouse` كان يفشل ويوقف الخط `&&` قبل أن
يصل الدور لـ`audit:axe`، فتوقفت إعادة التوليد تمامًا لا فحص واحد فقط).
`reports/delivery-readiness-standard-status.json` (٢٠٢٦-٠٧-١٠) و
`reports/release-deploy-rollback-status.json` (٢٠٢٦-٠٧-٠٨) نفس النمط.
القاعدة: **بوابة `test:*` تقرأ تقريرًا لها معنى فقط داخل نفس العملية
(نفس job) التي تُعيد `audit:*` توليد ذلك التقرير للتو — أي مستهلك آخر
(إنسان، تقرير، لوحة، أو بوابة موصولة في workflow مختلف) يقرأ `reports/*.json`
المحفوظ في git يقرأ أثرًا تاريخيًا، لا دليل حالة حالية أبدًا.** هذا سبب
إضافي — إلى جانب قسم ٥ — لماذا `scripts/run-publish-quality-gates.mjs` لا
يستخدم قائمة `&&` متسلسلة: توقف مبكر عند أول فشل يمنع كل `audit:*` بعده من
إعادة التوليد، فيصبح كل `test:*` بعده يقرأ تقريرًا محفوظًا قديمًا بصمت.
