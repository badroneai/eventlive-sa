# EVENTME — موجز منتج: بروفايل المدن (Destination Profiles)

**التاريخ:** 2026-07-19 | **الحالة:** **مُعتمد ومنفَّذ حتى المرحلة 3 جزئيًا** — أجازه المالك 2026-08-01 وشُحن حيًّا في اليوم نفسه (انظر «حالة التنفيذ» أسفل الوثيقة)

## الفكرة

المنصة اليوم تجيب على "ماذا يحدث" (فعاليات). بروفايل المدينة يضيف الإجابة على "إلى أين أذهب": لكل مدينة سعودية صفحة تجمع — إلى جانب فعالياتها — أهم أماكنها مصنفةً (طبيعة/ثقافة/ترفيه/مغامرة/طعام)، على نمط منشور وزير السياحة "الوجهة عسير". القيمة الاستراتيجية: محتوى دائم لا ينتهي، يُحيي المدن فقيرة الفعاليات (القصيم/الجنوب)، ويفتح مساحة SEO عربية ضخمة.

## المصادر المجانية — مرتبة (كلها فُحصت حيًّا)

| # | المصدر | الترخيص | الدور | ملاحظات |
|---|---|---|---|---|
| 1 | **Wikidata SPARQL** | CC0 (لا التزام إسناد) | **الهيكل الأساسي** — أسماء عربية+إنجليزية، تصنيف جاهز (instance-of)، إحداثيات | جُرّب على أبها وأرجع "قرية المفتاحة" نفسها من منشور الوزير. الفجوة: المدن الصغيرة بيانات رقيقة |
| 2 | **OpenStreetMap / Overpass** | ODbL — **إسناد إلزامي** "© OpenStreetMap contributors" على كل صفحة | **الكثافة والإحداثيات** — tourism/natural/historic/leisure tags | أغزر مصدر للمعالم الصغيرة والمطلات. يحتاج مراجعة بشرية (ضجيج) |
| 3 | **بيانات وزارة الثقافة المفتوحة** | مبادئ سدايا للبيانات المفتوحة | **فئة الثقافة** — 111 متحفًا + مواقع تراث ومسارح | moc.gov.sa/en/Modules/Pages/Qyias/OpenData |
| 4 | **مواقع هيئات التطوير** (discoveraseer.com نموذجًا) | لا ToS منشورة | **مرجع تحريري + روابط خارجية** — لا يُسحب | الموقع الرسمي نفسه بلا schema.org — هذه فجوتنا |
| 5 | **data.gov.sa** | مبادئ سدايا | مراقبة ربع سنوية — لا داتاسيت أماكن حاليًا | |
| 6 | Google Places API | **مدفوع** + قيود تخزين | مؤجل — طبقة إثراء مستقبلية فقط | |

### ⚠️ Visit Saudi — ممنوع السحب

شروط الهيئة السعودية للسياحة صريحة: المحتوى للاستخدام التحريري فقط، **يُمنع الاستخدام التجاري أو المرتبط بمنتج/علامة**. eventme منتج تجاري → السحب المباشر مخالفة. البديل: مرجع تحريري + روابط خارجية لهم، ومسار الشراكة الرسمي `partner.visitsaudi.com/en/partner-tools/api-center` عند الحاجة.

## نموذج البيانات

```
Place {
  id, name_ar, name_en, city_id, region_id,
  category: [nature|culture|entertainment|adventure|food],
  description_ar/en (تحريري أصلي 2-3 جمل — لا نسخ),
  coordinates, image {url, credit},
  source {origin: wikidata|osm|moc|manual, source_id, retrieved_at, license},
  official_link, status: [draft|reviewed|published], last_verified
}
City {
  id, name_ar/en, region_id, hero_image, intro_ar/en,
  places[] (مجمعة بالفئة), upcoming_events[] (من خط الفعاليات الحالي),
  seo {titles, meta, canonical}
}
```

**الربط بالفعاليات:** صفحة المدينة = بروفايل ثابت (يُحدَّث شهريًا/ربعيًا) + استعلام فعاليات حي بـ `city_id` من الكتالوج الحالي. بيانات الأماكن بطيئة التغير، الفعاليات سريعة — نفس بنية GitHub Pages الحالية بلا أي خادم.

**Schema.org:** صفحة المدينة → `TouristDestination` (بخاصية `includesAttraction`)، كل مكان → `TouristAttraction`، التجميع بالفئة → `ItemList`. **لا أحد من المنافسين يطبّق هذا للمدن السعودية** — ميزة rich results حقيقية.

## فرصة SEO

المتصدرون حاليًا لعائلات "أماكن سياحية في X" / "أفضل الأماكن في X" / "دليل X" / "وجهات X":
- **TripAdvisor** — محتوى مترجم غير أصيل، بلا تصنيف منسّق، بلا فعاليات.
- **Visit Saudi** — موثوق لكن غير محسّن للذيل الطويل العربي، وبلا طبقة فعاليات.
- **مواقع الهيئات الإقليمية** — محتوى جيد، صفر تجميع عابر للمدن، **صفر schema.org** (مؤكد بالفحص).

**الفجوة:** لا يوجد مركز واحد ثنائي اللغة، بترميز schema.org، يجمع بروفايل الوجهة المصنّف + طبقة فعاليات حية. هذه هي.

## الحوكمة

- كل Place يحمل `source` قابلاً للتدقيق (Q-id أو node-id أو صفحة حكومية) — **لا اختلاق أبدًا**.
- إسناد OSM ظاهر على كل صفحة تستخدم بياناته. Wikidata إسناد مجاملة.
- **بوابة مراجعة بشرية قبل النشر** — المسحوب مرشّحات لا نسخ نهائية؛ الوصف العربي يُكتب بصوت المنصة.
- حقل `last_verified` وإعادة فحص دورية (الأماكن تغلق وتتغير).

## الخطة المرحلية

- **المرحلة 0 — تجربة بمنطقة واحدة:** عسير (أغنى مرجع، تطابق مباشر مع منشور الوزير) أو **القصيم** (أعلى قيمة استراتيجية — تثبت أطروحة "القيمة الدائمة للمدن فقيرة الفعاليات"، وسجل الحسابات أثبت غياب أي موسم/مهرجانات موثقة لها). ترشيح PM: **القصيم**. تنسيق: 5–8 أماكن × 4–5 فئات، تحرير يدوي من مسودات Wikidata/OSM/وزارة الثقافة.
- **المرحلة 1 — الأدوات:** سكربت جلب (SPARQL + Overpass + MOC) → **طابور مراجعة** لا طابور نشر؛ إدماج Place/City في مولّد الموقع + JSON-LD.
- **المرحلة 2 — التوسع للمدن فقيرة الفعاليات أولًا:** القصيم، نجران، الباحة، جازان، حائل.
- **المرحلة 3 — التعميم الوطني + الدمج:** كل مدينة صفحة؛ صفحات المدن مدخل أساسي (وجهة + فعاليات)؛ قياس النجاح بترتيب SEO على عائلات الاستعلام المستهدفة.
- **المرحلة 4 (مستقبلًا):** شراكة Visit Saudi API؛ تقييم Google Places لإثراء محدود.

## حالة التنفيذ (يُحدَّث مع كل جولة)

أُجيز الموجز من المالك في 2026-08-01 وشُحن في اليوم نفسه. الوضع القائم عند 2026-08-02: **310 معلمًا عبر 36 مدينة**.

| المرحلة | الحالة | الأثر في الشيفرة |
|---|---|---|
| 0 — تجربة منطقة واحدة | **تُخطّيت** — نُفِّذت 30 مدينة دفعة واحدة (239 معلمًا) بدل منطقة واحدة | PR #65 (البنية) + #66 (البيانات) |
| 1 — الأدوات | **منفَّذة** | `data/city_places.json` + المخطط + `scripts/validate-city-places.mjs` (داخل `npm run validate`)، `scripts/city-places-render.mjs`، `place-category-taxonomy.mjs`، `city-name-registry.mjs`، بوابة `npm run test:city-places` |
| 2 — المدن فقيرة الفعاليات أولًا | **منفَّذة** — القصيم أولًا كما رشّح الموجز | PR #67 (+31) و #71 (+40 من مرجع إمارة القصيم، 6 صفحات قصيمية جديدة) |
| 3 — التعميم الوطني | **الفتح البنيوي تم** — المدينة لم تعد تحتاج فعالية لتنال صفحة | PR #70: `placesOnlyCitySlugs` + عقد سجل يفشل بصوت عالٍ لأي مدينة بلا مدخل + حالة «لا فعاليات حالية» صادقة |
| — إثراء الصور (حقل `Place.image` في الموجز) | **منفَّذ** | PR #69: Wikidata P18 → Commons، **رخص حرة فقط**، تخزين محلي في `dist/assets/place-images` + `data/place_image_manifest.json`، سطر إسناد ظاهر، موصول في `sources:sync` |
| 4 — شراكة Visit Saudi / Google Places | **لم تبدأ** — إجراء ملكية (قرار Q3 في لوحة المهام) | — |

**التزامات الحوكمة المطبَّقة فعليًا:** كل مكان يحمل مصدرًا قابلًا للتدقيق؛ إسناد OSM ظاهر حيث استُخدم؛ إسناد الصور (الفنان · الرخصة · رابط Commons) تحت كل صورة كما تفرض CC؛ الأوصاف العربية بصوت المنصة لا منسوخة؛ Visit Saudi و Saudipedia و alqassim.gov.sa **مراجع تحقّق وروابط فقط، لا يُنسخ منها نص ولا صورة ولا إحداثيات**.

**دين مفتوح على هذه الطبقة:** لا أتمتة بعد لإيقاع إعادة التحقق الدوري لحقل `last_verified` (المرحلة 1 من الحوكمة أعلاه)؛ ومدن رقيقة يحذّر منها المدقّق تحذيرًا ليّنًا (تبوك، مكة، سكاكا، النماص، الأفلاج) بانتظار جولة إثراء لاحقة.

## مراجع

- Wikidata: https://query.wikidata.org/sparql | Overpass: https://overpass-api.de/api/interpreter
- Visit Saudi ToS: https://www.visitsaudi.com/en/privacy-policy/terms-and-conditions
- STA Open Data: https://www.sta.gov.sa/en/open-data | MOC Open Data: https://www.moc.gov.sa/en/Modules/Pages/Qyias/OpenData
- نموذج إقليمي: https://discoveraseer.com | schema.org: TouristDestination / TouristAttraction
